package utility;
/** Functional interface for lambda expressions */
public interface Checking {
    /** Abstract method for lambda expressions. */
    public abstract boolean meetsTwoConditions(boolean first, boolean second);
}
